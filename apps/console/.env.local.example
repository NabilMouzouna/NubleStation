# Copy to .env.local for local development (outside Docker).
# .env.local is gitignored — never commit it.

# Point the console at a local SQLite file instead of the Docker bind mount
ADMIN_DB_PATH=./dev.db
